var shortID = require('shortid');
var vector2 = require('./Vector2');
module.exports= class Room{
    constructor() {
        id=shortID.generate();
        currentuser=new Number(0);
        maxuser=new Number(0);

    }}